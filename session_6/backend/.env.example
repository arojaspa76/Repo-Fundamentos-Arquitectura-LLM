# Copie a .env y complete solo las API keys que vaya a probar con llamadas
# reales. Sin key, ese proveedor corre en MODO_SIMULADO (ver proveedores.py)
# — el pipeline completo (costeo, latencia, bias probe, selección) funciona
# igual sin ninguna credencial.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

CORS_ORIGINS=http://localhost:5173
